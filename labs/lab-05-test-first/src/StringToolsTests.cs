using Xunit;

namespace Lab05.StringTools.Tests;

public class StringToolsTests
{
    // TODO: A method CollapseWhitespace(string input) -> string
    // Behavior intentions:
    // 1. Null or empty input returns empty string
    // 2. Leading/trailing whitespace trimmed
    // 3. Internal runs of whitespace (space, tab, newline) collapsed to single space
    // 4. Unicode non-breaking spaces treated as regular spaces
    // 5. Preserves internal casing and punctuation

    // Prompt Copilot: "Generate xUnit tests for the intentions above including edge cases." 
    // After generation, refine naming and ensure clarity.
}
